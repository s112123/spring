<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	
	<!-- 회원전체조회 -->
	<select id="selectAll" resultType="com.spring.app.entity.Member">
		SELECT
			*
		FROM member ORDER BY id DESC
	</select>

	<!-- 회원등록 -->
	<insert id="insert">
		INSERT INTO member 
		VALUES(NULL, #{email}, #{username}, #{passwd}, #{phone}, #{addr1}, #{addr2}, DEFAULT, DEFAULT, NOW())
	</insert>
	
	<!-- 회원조회: id -->
	<select id="selectOneById">
		SELECT
			*
		FROM member WEHRE id=#{id}
	</select>
</mapper>
